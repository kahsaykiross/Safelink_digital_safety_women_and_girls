name: safelink_safety_toolkit
description: A Flutter-based digital safety toolkit for women and girls, built for PowerHacks 2025.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.20.0
  firebase_auth: ^4.8.0
  cloud_firestore: ^4.10.0

  # Utilities
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true